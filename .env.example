# Supabase — required for the auth-aware nav header.
# Copy these from the mermaid-studio .env.local (VITE_SUPABASE_* → NEXT_PUBLIC_SUPABASE_*).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
